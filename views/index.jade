extends layout

block content
  header
    h1= title
    p Welcome to #{title}
  
  main 
    a(href='/new') Add a new message

    each message in messages
      h2= message.user
      h3= message.text
      h4= message.added
